var assert = require('assert');
describe('middlewares', function () {
  var middlewares = require('../');
  Object.keys(middlewares).forEach(function(name) {
    it(name + ' should be a Function', function() {
      assert.ok(middlewares[name] instanceof Function);
    });
  });
});
